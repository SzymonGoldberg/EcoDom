<main class="container">
  <div class="header">
    <h1>Rooms</h1>
    <%= link_to "New room",
                new_room_path,
                class: "btn btn--primary",
                data: { turbo: false } %>
  </div>

  <%= render @rooms %>
</main>